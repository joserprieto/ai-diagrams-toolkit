# ============================================================================
# AI Diagrams Toolkit - Environment Configuration (Distribution)
# ============================================================================
# This file contains default configuration values for Makefile.
#
# Usage:
#   1. Copy to .env: cp .env.example .env (optional)
#   2. Edit .env to override any value
#   3. .env is gitignored (local customization)
#
# Note: Makefile reads from .env if exists, otherwise uses these defaults
#       Shell scripts can also source this file directly
#
# IMPORTANT: Use shell syntax (VAR=value) NOT Makefile syntax (VAR := value)
#            This allows both Makefile and shell scripts to use this file
# ============================================================================

# ── Core Tools ──────────────────────────────────────────────────────────────
# System tools used by Makefile targets
# Override these if you use alternative tools (e.g., hg instead of git)

GIT=git
NPX=npx
JQ=jq
CLAUDE=claude

# ── Semantic Executors ──────────────────────────────────────────────────────
# High-level abstractions for swappable executors
# These use the core tools by default but can be overridden independently

# AI CLI executor (for test/commands target)
# Options: claude, cursor-agent, junie, codex (v0.4.0+)
# NOTE: Use full path if using shell aliases (aliases don't work in non-interactive bash)
# Find path: which claude (in your interactive shell)
AI_CLI_EXECUTOR=claude

# Node package executor (for release targets)
# Options: npx, bunx, pnpm dlx
NODE_EXECUTOR=npx

# Release automation tool
# Currently: commit-and-tag-version (see NODE_RELEASE_PACKAGE_VERSION below)
RELEASE_TOOL=commit-and-tag-version

# ── Dependency Checking Behavior ────────────────────────────────────────────
# Control when dependency validation runs
# true  = Always check dependencies before running targets (default, safe)
# false = Skip dependency checks (useful in CI after first validation)
CHECK_DEPS=true

# ── Release Tooling ─────────────────────────────────────────────────────────
# Package used for automated semantic versioning and CHANGELOG generation
NODE_RELEASE_PACKAGE=commit-and-tag-version
NODE_RELEASE_PACKAGE_VERSION=12.4.4

# Configuration file for release tool (behavior config)
# Default: .versionrc.js (commit-and-tag-version auto-detects this)
# Override if you want to use a different config file
NODE_RELEASE_CONFIG=.versionrc.js

# Alternative tools (comment/uncomment to switch):
# NODE_RELEASE_PACKAGE := standard-version
# NODE_RELEASE_PACKAGE_VERSION := 9.5.0
# NODE_RELEASE_CONFIG := .versionrc.js

# ── Future Tooling (Examples) ───────────────────────────────────────────────
# Uncomment when/if needed

# Diagram validation
# NODE_DIAGRAM_VALIDATOR := mermaid-cli
# NODE_DIAGRAM_VALIDATOR_VERSION := 10.6.1

# Markdown linting
# NODE_MARKDOWN_LINTER := markdownlint-cli
# NODE_MARKDOWN_LINTER_VERSION := 0.37.0
